services:
  postgis:
    environment:
      - POSTGRES_USER=rm
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD environment variable is required}
      - POSTGRES_DB=rm
    deploy:
      resources:
        limits:
          cpus: '3'
          memory: 5G
